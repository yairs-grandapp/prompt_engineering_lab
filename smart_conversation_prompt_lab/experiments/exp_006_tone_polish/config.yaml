experiment:
  name: "Tone Polish - Warmer false alarm relief, sensor verbalization, severe injury escalation"

models:
  - name: "gpt-4o-mini"
    temperature: 0.7
  - name: "gpt-4o"
    temperature: 0.7

prompt:
  template: "prompt_v5.txt"

conversation:
  max_turns: 10
  extra_silence_turns: 2

inputs_file: "inputs_v0.json"

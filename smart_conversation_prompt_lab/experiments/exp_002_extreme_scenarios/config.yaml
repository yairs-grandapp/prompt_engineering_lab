experiment:
  name: "Extreme Scenarios - Fall Detection with Severe Inputs"

models:
  - name: "gpt-4o-mini"
    temperature: 0.7
  - name: "gpt-4o"
    temperature: 0.7

prompt:
  template: "prompt_v1.txt"

conversation:
  max_turns: 10
  extra_silence_turns: 2

inputs_file: "inputs_v0.json"

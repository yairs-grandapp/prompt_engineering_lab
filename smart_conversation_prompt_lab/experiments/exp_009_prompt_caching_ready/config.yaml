experiment:
  name: "Prompt Caching Ready - Senior name at end, no assistant name, production-aligned"

models:
  - name: "gpt-4o-mini"
    temperature: 0.3
  - name: "gpt-4o"
    temperature: 0.7

prompt:
  template: "prompt_v8.txt"
  senior_name: "Jake"

conversation:
  max_turns: 10
  extra_silence_turns: 2
  enable_state_injection: false

inputs_file: "inputs_v0.json"

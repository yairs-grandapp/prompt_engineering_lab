experiment:
  name: "Tiered Escalation - Immediate for emergencies, one check for confusion, double check for silence"

models:
  - name: "gpt-4o-mini"
    temperature: 0.3
  - name: "gpt-4o"
    temperature: 0.7

prompt:
  template: "prompt_v6.txt"

conversation:
  max_turns: 10
  extra_silence_turns: 2

inputs_file: "inputs_v0.json"

experiment:
  name: "V6+V4 Critical Fixes - Mandatory Calculation + Pattern Verification"
  date: "2025-10-15"
  description: |
    Implements three critical fixes from test results analysis:
    - Fix #1: Mandatory calculation block (daily v6)
    - Fix #2: Pattern verification with start/end check (weekly v4)
    - Fix #3: Boundary case clarification (daily v6)

    Expected improvement: 73% → 90%+ for daily summaries
    Expected fix: Eliminate contradictions in weekly summaries

model:
  name: "gpt-4o"
  temperature: 0.7

prompts:
  daily_template: "daily_summary_v6.txt"  # Mandatory calculation + boundary cases
  weekly_template: "weekly_summary_v4.txt"  # Pattern verification

test_data:
  senior_id: "A_LK611111SGPD_senior_1744703199152"

  behaviors:
    - category: "bathroom"
      stat_id: "hygiene usage"
    - category: "sleep"
      stat_id: "Sleep duration"
    - category: "distress"
      stat_id: "Falls detected"

  date_range:
    start: "2025-09-05"
    end: "2025-09-09"  # 5 days - same as v5/v3 for direct comparison
